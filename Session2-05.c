#include <stdio.h>

int main(){
    int length = 10;
    int width = 20;
    int area = length * width;
    int perimeter = (length + width)*2;
    printf("Diện tích hình chữ nhật là: %d\n",area);
    printf("Chu vi hình chữ nhật là: %d",perimeter);
}